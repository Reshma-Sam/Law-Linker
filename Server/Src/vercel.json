{
  "version": 2,
  "builds": [
    { "src": "Server/Src/Server.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/",
      "method": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}
